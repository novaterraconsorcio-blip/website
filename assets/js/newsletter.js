const formNewsletter=document.querySelector('[data-newsletter]');if(formNewsletter){formNewsletter.addEventListener('submit',async(e)=>{e.preventDefault();const fd=new FormData(formNewsletter);const email=String(fd.get('email')||'').trim();const status=formNewsletter.querySelector('.form-status');if(!/^\S+@\S+\.\S+$/.test(email)){status.textContent='Email inválido';return}try{const list=JSON.parse(localStorage.getItem('newsletter')||'[]');list.push({email,ts:Date.now()});localStorage.setItem('newsletter',JSON.stringify(list));status.textContent='Inscrito com sucesso'}catch{status.textContent='Falha ao inscrever'}})}