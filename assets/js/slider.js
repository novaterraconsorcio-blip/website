function getSlides(){try{const raw=localStorage.getItem('heroSlides');if(!raw)return null;const arr=JSON.parse(raw);if(Array.isArray(arr)&&arr.length)return arr}catch{}return null}
function defaultSlides(){return[{title:'Nova Terra Consórcio',subtitle:'Realize Seus Sonhos',ctaText:'Simular agora',ctaHref:'simulacao.html'},{title:'A forma mais inteligente e econômica',subtitle:'Para conquistar seu imóvel, veículo ou projetos',ctaText:'Falar com especialista',ctaHref:'contato.html'},{title:'Por Que Escolher a Nova Terra?',subtitle:'Transparência, planejamento e economia',ctaText:'Saiba mais',ctaHref:'sobre.html'}]}
function renderSlides(wrapper){const slides=getSlides()||defaultSlides();wrapper.innerHTML='';for(const s of slides){const el=document.createElement('div');el.className='hero-slide';el.innerHTML=`<h1>${s.title}</h1><p>${s.subtitle}</p><a class="btn btn-primary" href="${s.ctaHref}">${s.ctaText}</a>`;wrapper.appendChild(el)}let idx=0;setInterval(()=>{idx=(idx+1)%slides.length;wrapper.style.transform=`translateX(-${idx*100}%)`;wrapper.style.transition='transform .6s ease'},4000)}
const slider=document.querySelector('[data-slider]');if(slider){renderSlides(slider)}