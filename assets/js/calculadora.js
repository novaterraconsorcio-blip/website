function brl(n){return n.toLocaleString('pt-BR',{style:'currency',currency:'BRL'})}
const formCalc=document.querySelector('[data-calc]');if(formCalc){formCalc.addEventListener('submit',(e)=>{e.preventDefault();const fd=new FormData(formCalc);const valor=Number(fd.get('valor')||0);const prazo=Number(fd.get('prazo')||1);const taxa=Number(fd.get('taxa')||0);const parcela=valor/Math.max(1,prazo)+(taxa/100)*valor/Math.max(1,prazo);const total=parcela*prazo;document.querySelector('[data-out-parcela]').textContent=`Parcela estimada: ${brl(parcela)}`;document.querySelector('[data-out-total]').textContent=`Total aproximado: ${brl(total)}`;const status=formCalc.querySelector('.form-status');status.textContent='Cálculo atualizado'})}